const { createElectionKeySchema } = require('../src/lib/validators.ts');
// Note: We cannot directly run TS in Node without ts-node/register, but we can write a TS/JS script and run it using ts-node or vitest.
// Let's create a vitest test or run a quick script.
